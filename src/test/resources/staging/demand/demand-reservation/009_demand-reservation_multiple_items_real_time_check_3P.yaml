arrange:
  - name: Add Stock for multi line Items + same Node code
    type: api
    act:
      url: https://hd-fulfillment-global-supply-demand-service.ecomm-stg.cencosud.com/v1/supplies/update
      method: PUT
      body:
        businessUnit: PARIS
        countryCode: CL
        items:
          - id: MKR8VRTKT1-1
            nodeCode: a1ec1668-f67c-4089-905b-f906ee22db76
            supplyType: ATP
            updateType: ABSOLUTE
            quantity:
              value: 2
              unitOfMeasure: UNITS
            condition: NEW
            updatedAt: "${generate:datetime:now}"
          - id: MK2NHMYUVC-2
            nodeCode: c6a52e7c-f3e7-4149-b231-d376be9a4420
            supplyType: ATP
            updateType: ABSOLUTE
            quantity:
              value: 2
              unitOfMeasure: UNITS
            condition: NEW
            updatedAt: "${generate:datetime:now}"
    saveResponseAs: stock

act:
  url: https://hd-fulfillment-global-supply-demand-service.ecomm-stg.cencosud.com/v1/demands
  method: POST
  headers:
    X-Business-Unit:
      - PARIS
    X-Country-Code:
      - CL
  body:
    meta:
      referenceId: "${generate:string}"
      orderId: "${generate:string}"
      orderType: CUSTOMER_ORDER
      expiryInMilliseconds: 120000
      channel: MOBILE
      geoCode: "13114"
      geoType: COMUNA
    reservations:
      - itemId: "${stock.successfulItems[0].id}"
        condition: "${stock.successfulItems[0].condition}"
        promiseDeliveryDate: "${generate:datetime:tomorrow}"
        quantity:
          value: 10
          unitOfMeasure: "${stock.successfulItems[0].quantity.unitOfMeasure}"
        nodeCode: "${stock.successfulItems[0].nodeCode}"
        riskLevel: HIGH
        orderLineId: "3"
        sellerType: 3P
        logisticSize: SMALL
        shipping:
          type: DELIVERY
          subType: HOME
          method: NEXT_DAY
      - itemId: "${stock.successfulItems[1].id}"
        condition: "${stock.successfulItems[1].condition}"
        promiseDeliveryDate: "${generate:datetime:tomorrow}"
        quantity:
          value: 10
          unitOfMeasure: "${stock.successfulItems[1].quantity.unitOfMeasure}"
        nodeCode: "${stock.successfulItems[1].nodeCode}"
        riskLevel: HIGH
        orderLineId: "4"
        sellerType: 3P
        logisticSize: SMALL
        shipping:
          type: DELIVERY
          subType: HOME
          method: NEXT_DAY


asserts:
  status: 422
  customization:
    body:
      options:
        - IGNORING_EXTRA_FIELDS
        - IGNORING_VALUES
  body: {}